@page "/tasks"
@page "/tasks/{TaskId}"
@using Atlas.Ui.Components.Tasks
<PageTitle>Tasks — Atlas</PageTitle>

<div class="page pageFill">
    <PageTitleRow Title="Tasks" AddLabel="Add task" AddingLabel="Adding…" Adding="_creating" OnAdd="HandleAddTask" />

    <div class="tasksStack @(IsFocusMode ? "tasksStackFocus" : null) @(!ShowDetail ? "tasksStackNoDetail" : null)">
        @if (!IsFocusMode)
        {
            <section class="card tight tasksFiltersRow" aria-label="Task filters">
                <label class="field">
                    <div class="fieldLabel">Project</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_projectFilter" @onchange="OnProjectFilterChange">
                            <option value="All">All</option>
                            <option value="">(None)</option>
                            @foreach (string p in ProjectOptions)
                            {
                                <option value="@p">@p</option>
                            }
                        </select>
                        <button type="button" class="btn btnGhost btnIcon" title="Sort by Project" @onclick='() => ToggleSort("Project")'>
                            @SortButtonGlyph("Project")
                        </button>
                    </div>
                </label>

                <label class="field">
                    <div class="fieldLabel">Risk</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_riskFilter" @onchange="OnRiskFilterChange">
                            <option value="All">All</option>
                            <option value="">(None)</option>
                            @foreach (string r in RiskOptions)
                            {
                                <option value="@r">@r</option>
                            }
                        </select>
                        <button type="button" class="btn btnGhost btnIcon" title="Sort by Risk" @onclick='() => ToggleSort("Risk")'>
                            @SortButtonGlyph("Risk")
                        </button>
                    </div>
                </label>

                <label class="field">
                    <div class="fieldLabel">Priority</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_priorityFilter" @onchange="OnPriorityFilterChange">
                            <option value="All">All</option>
                            <option value="Low">Low</option>
                            <option value="Medium">Medium</option>
                            <option value="High">High</option>
                            <option value="Critical">Critical</option>
                        </select>
                        <button type="button" class="btn btnGhost btnIcon" title="Sort by Priority" @onclick='() => ToggleSort("Priority")'>
                            @SortButtonGlyph("Priority")
                        </button>
                    </div>
                </label>

                <label class="field">
                    <div class="fieldLabel">Status</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_statusFilter" @onchange="OnStatusFilterChange">
                            <option value="All">All</option>
                            <option value="Not Started">Not Started</option>
                            <option value="In Progress">In Progress</option>
                            <option value="Blocked">Blocked</option>
                            <option value="Done">Done</option>
                        </select>
                    </div>
                </label>

                <label class="field">
                    <div class="fieldLabel">Staleness</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_stalenessFilter" @onchange="OnStalenessFilterChange">
                            <option value="All">All</option>
                            <option value="Fresh">Fresh</option>
                            <option value="Warning">Getting close</option>
                            <option value="Stale">Stale</option>
                        </select>
                        <button type="button" class="btn btnGhost btnIcon" title="Sort by Staleness" @onclick='() => ToggleSort("Staleness")'>
                            @SortButtonGlyph("Staleness")
                        </button>
                    </div>
                </label>

                <label class="field">
                    <div class="fieldLabel">Est. Duration</div>
                    <div class="fieldInline">
                        <select class="select selectCompact" value="@_durationFilter" @onchange="OnDurationFilterChange">
                            <option value="All">All</option>
                            <option value="&lt;=30m">≤ 30m</option>
                            <option value="&lt;=2h">≤ 2h</option>
                            <option value="&lt;=4h">≤ 4h</option>
                            <option value="&lt;=1d">≤ 1d</option>
                            <option value="&gt;1d">&gt; 1d</option>
                            <option value="Invalid">Invalid</option>
                        </select>
                        <button type="button" class="btn btnGhost btnIcon" title="Sort by Estimated Duration" @onclick='() => ToggleSort("Estimated Duration")'>
                            @SortButtonGlyph("Estimated Duration")
                        </button>
                    </div>
                </label>
            </section>
        }

        @if (!IsFocusMode)
        {
            <section class="list listCard tasksListRow @(ShowDetail && FilteredSorted.Count > 5 ? "tasksListCapped" : null)"
                     aria-label="Task list">
                @foreach (AtlasTask t in FilteredSorted)
                {
                    Guid taskId = t.Id;
                    string statusLabel = DisplayLabels.FormatTaskStatus(t.Status);
                    int days = DisplayLabels.DaysSince(t.LastTouchedIso) ?? 0;
                    string activity = ActivityColor(days);
                    int depCount = BlockedByCount(t);
                    <button @key="taskId"
                            type="button"
                            class="listRow listRowBtn tasksTaskRow @(taskId == Selection.SelectedTaskId ? "listRowActive" : null)"
                            @onclick="() => SelectFromList(taskId)"
                            @ondblclick="() => GoFocus(taskId)">
                        <div class="listMain">
                            <div class="listTitle listTitleWrap">@t.Title</div>
                            <div class="listMeta listMetaWrap">
                                @if (!string.IsNullOrEmpty(t.Project)) { <text> • @t.Project</text> }
                                @if (!string.IsNullOrEmpty(t.Risk)) { <text> • Risk: @t.Risk</text> }
                                @if (depCount > 0) { <text> • Blocked by @depCount</text> }
                            </div>
                            <div class="listMeta listMetaWrap">
                                @(string.IsNullOrEmpty(t.DueDate) ? " " : $"Due {t.DueDate}")
                            </div>
                        </div>
                        <div class="pillRow tasksTaskRowPills">
                            <div class="pill">@ListDurationPill(t.EstimatedDurationText)</div>
                            <div class="pill @Tones.TaskStatusTone(t.Status)">@statusLabel</div>
                            <div class="pill @Tones.PriorityTone(t.Priority)">@t.Priority</div>
                            <span class="activityDot activityDot-@activity"
                                  title="Activity: @days d ago (stale threshold @StaleDays d)"
                                  aria-label="Activity: @days days ago"></span>
                        </div>
                    </button>
                }
                @if (FilteredSorted.Count == 0)
                {
                    <div class="muted pad">No tasks match your filters.</div>
                }
            </section>
        }

        @if (ShowDetail)
        {
            <TaskDetail Task="Selected"
                        IsFocusMode="IsFocusMode"
                        AutoEditId="_autoEditId"
                        AutoEditCleared="ClearAutoEdit"
                        OnClose="CloseDetail"
                        OnEnterFocus="EnterFocus"
                        OnExitFocus="ExitFocus"
                        OnDeleted="OnTaskDeleted" />
        }
    </div>
</div>
